#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../jison/lib/jison/jison2json.js" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../jison/lib/jison/jison2json.js" "$@"
  ret=$?
fi
exit $ret
